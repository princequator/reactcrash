/**
 * State :
    * new state returned from the setState doesn't merge with the prev state instead it overwrites the previos state.
    * Return in useEffect hook is used to cleanup side effects.
    * In setState, Use function instead of a direct value.
     */